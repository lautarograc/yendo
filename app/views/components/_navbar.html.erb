<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid" id="contenedor-menu">
    <a class="navbar-brand" href="#">
      <button class="btn btn-default" onclick="window.location.href='/'" >
        <%= image_tag "logo.png", :id => "imglogo" %>
      </button>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarSupportedContent">
      <%= form_tag(stores_path, method: :get, class: 'form-inline' ) do %>
        <div class="input-group">
          <span class="input-group-text" id="basic-addon1"><i class="bi bi-search"></i></span>
          <%= text_field_tag(:search, params[:search], class: 'form-control', placeholder: "Buscá local o comida") %>
          <%= submit_tag ("Search") , class: 'btn btn-outline-success' %>
        </div>
      <% end %>
      <ul class="navbar-nav">
        <% if current_user %>
          <li class="nav-link fw-bold text-nowrap">
            <span> <i class="bi bi-person"></i> Hola <%= current_user.name%>! </span>
          </li>
        <% end %>
        <li class="nav-item">
          <a class="nav-link icon" href="#" id="location"><i class="bi bi-geo-alt"></i><span class="ubicacion"><%= get_location %></span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#" id="profile"><i class="bi bi-person icon"></i></a>
        </li>
        <li class="nav-item">
          <%= link_to "", shopping_cart_path, class:" nav-link bi bi-cart icon text fs-5" %>
        </li>
      </ul>
    </div>
  </div>
</nav>
